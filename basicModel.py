
from ticket import Ticket
import random

class Model(object):
    """docstring forBasic."""
    self.maximumSprintTickets = 15
    self.teamsTicketCapacity = 6

    def __init__():
        self._tickets = []
        self._ticketIndexs = [] # order of the tickets according to
                                # their buisinesValue
    def _generateNumberOfTicketsNextSprint():
        # warning random not state check
        return random.randrange(maximumSprintTickets)

    def _generateTickets(n):
        tickets = []
        for i in ragne(n):
            tickets.append(Ticket())
        return tickets

    def selectTickets(n):
        # order a
